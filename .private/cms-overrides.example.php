<?php
declare(strict_types=1);

/**
 * Copy to cms-overrides.php in this folder (same directory as this example).
 * cms-overrides.php is gitignored — safe for reset_email and updated password hashes.
 */
return [
    // Optional: if set, login must use this username (with password). Leave unset or empty to allow any username.
    // 'admin_username' => 'admin',
    // 'reset_email' => 'you@example.com',
    // 'reset_mail_from' => 'noreply@yourdomain.com',
    // After a password reset, admin_password_hash may be written here automatically.
];
